import { createSlice } from '@reduxjs/toolkit';

interface SearchFiltersType {}

const patientSearchFiltersSlice = createSlice({
  name: 'patientSearchFilters',
  initialState: {},
  reducers: {},
});

export default patientSearchFiltersSlice;
